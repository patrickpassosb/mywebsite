{
  "name": "mywebsite",
  "version": "1.0.0",
  "description": "Personal website with live reload development server",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html --watch=.",
    "start": "live-server --port=3000 --open=/index.html --watch=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}

